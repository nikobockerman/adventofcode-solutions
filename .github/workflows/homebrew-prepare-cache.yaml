name: Homebrew - prepare cache
on:
  workflow_call:
    inputs:
      runner:
        description: Runner to use
        required: true
        type: string
    outputs:
      packages-hash:
        description: Hash of installed packages
        value: ${{ jobs.prepare-cache.outputs.packages-hash }}
  workflow_dispatch:
    inputs:
      runner:
        default: ubuntu-24.04
        description: Runner to use
        required: true
        type: string
permissions:
  contents: read

jobs:
  prepare-cache:
    name: Prepare Homebrew cache
    outputs:
      packages-hash: ${{ steps.prepare-cache.outputs.packages-hash }}
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - id: prepare-cache
        name: Prepare cache
        uses: ./.github/actions/homebrew-packages-setup
        with:
          cache-mode: prepare
